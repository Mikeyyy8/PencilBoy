// eslint-disable-next-line no-unused-vars
import { motion } from "framer-motion"

const Categories = () => {
    
  return (
    <motion.aside className="w-32 left-0">
        <motion.div>
            this is where the categories will be lined up
        </motion.div>
    </motion.aside>
  )
}

export default Categories